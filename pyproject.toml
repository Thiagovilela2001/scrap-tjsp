[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "scraping-tjsp"
version = "0.2.0"
description = "Coletor responsável de jurisprudência pública do TJSP (CJSG)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12,<5",
  "chromadb>=1.5,<2",
  "fastapi>=0.139,<1",
  "openai>=2,<3",
  "pymupdf>=1.26,<2",
  "python-dotenv>=1,<2",
  "requests>=2.32,<3",
  "uvicorn>=0.51,<1",
]

[project.optional-dependencies]
dev = ["httpx>=0.28,<1", "pytest>=8,<9", "ruff>=0.12,<1"]
embeddings = ["sentence-transformers>=5.6,<6"]

[project.scripts]
tjsp-jurisprudencia = "scraping_tjsp.cli:main"
tjsp-busca = "scraping_tjsp.search_cli:main"
tjsp-avaliar = "scraping_tjsp.evaluation_cli:main"
tjsp-auditoria = "scraping_tjsp.audit_cli:main"
tjsp-preparar-dataset = "scraping_tjsp.dataset_cli:main"
tjsp-comparar-embeddings = "scraping_tjsp.embedding_comparison_cli:main"
tjsp-api = "scraping_tjsp.api_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
scraping_tjsp = ["schema_sqlite.sql", "web/*.css", "web/*.html", "web/*.js"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["src"]
markers = ["integration: acessa serviços externos reais; exige --run-integration"]

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "RUF", "UP"]
